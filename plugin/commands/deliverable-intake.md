# Deliverable intake

Read the deliverable's unprocessed `inbox/` captures (or import chosen entries from the macwhspr dictation log with `--from-log N`), classify every statement as a decision, evidence claim, question, task, or dropped-with-reason, trace each to the capture, leave uncertain names and citations UNRESOLVED, and show one unified diff against the planning wiki. Apply only on assent; decisions always need it. Then rename the capture `*.done.md` and log one line in the top HANDOFF entry. See the `deliverable-intake` skill.

$ARGUMENTS
