# Diverge → Codex

Alias for `/oss:diverge --codex`. Run the `diverge` skill in its Codex mode: Codex (GPT-5.6 "Sol" at xhigh effort) generates the approaches and, after the user selects one, implements it. Force `--codex` even if the arguments below do not name it.

$ARGUMENTS
