# Deliverable open

Open the deliverable named below as a pipeline unit: interview the author in rounds (kind and root, audience and deadline, claim and done-test, sources of truth, sections, checks, planning wiki, voice pin), then write `deliverable.yml`, an append-only `HANDOFF.md`, the `planning/` wiki index and open-questions file, `inbox/`, and a gitignored `checks/`. For a talk with no sources, copy the house deck template. Offer, and only on a yes, install the pre-commit gate, append proper nouns to the macwhspr vocabulary, and register the manifest with the night shift (listed, not enabled). Finish by running the gate once. See the `deliverable-open` skill.

$ARGUMENTS
