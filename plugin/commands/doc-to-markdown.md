# Document to Markdown

Read or convert the document below. Decide whether to read it directly or convert it, route to the right converter based on the document's actual structure, guard against each converter's known failure modes, and decide whether the resulting Markdown is a tracked artifact or a scratch file to delete.

$ARGUMENTS
