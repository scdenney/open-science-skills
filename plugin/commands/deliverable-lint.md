# Deliverable lint

Review the whole deliverable, detection only. Run the deterministic gate first and stop on a failure; then one Sonnet finder per section in parallel (clarity and voice, claim-to-source, consistency); then one Opus pass over the assembled whole (order, spec conformance, repetition, release rules); then adjudicate: dedupe, verify every anchor, rank P0/P1/P2, require evidence for any fabrication flag, and record honest NOT-CHECKED coverage under the dollar budget (default 3). Write `checks/<date>/findings.json` and `report.md`. Never edit a source file. See the `deliverable-lint` skill.

$ARGUMENTS
