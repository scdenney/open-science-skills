interface:
  display_name: "Orchestrate"
  short_description: "Let Astra lead parallel Codex workstreams"
  default_prompt: "Use $orchestrate to lead and verify this task across Codex subagents."
policy:
  allow_implicit_invocation: false
