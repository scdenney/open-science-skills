CLAUDE.md
setup-sync.sh
.claude/
.DS_Store

# Knowledge base and conversion tooling: kept local only (synced via Google Drive, not git)
# (anchored: skill-level scripts/ dirs, e.g. plugin/skills/paper-tex/scripts/, ARE tracked)
knowledge_base/
/scripts/
*.pdf

# Sources corpus: PDFs + MD conversions kept local (size + copyright)
sources/

# Python venv used for PDF conversion
.venv/
__pycache__/
*.py[cod]

.syncthing.*.tmp
